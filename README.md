# Bit-Based-Windows-C-ESP32-Communication
A Windows C application that sends binary data to an ESP32 over USB Serial using the Win32 API. Built with low-level serial communication and designed as a base for custom hardware control and embedded projects
